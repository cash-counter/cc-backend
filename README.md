# Cash Counter - Backend

## Starting up the server
To startup the server first install required dependencies running:

`npm install`

Then start the server with the following command:

`node startup`

